Algoritmo "FIXAR EST PARA"

Var

   i, numero, tabuada: inteiro

Inicio

   escreva("Deseja imprimir na tela a tabuada de qual numero? ")
   leia(numero)
   
   para i de 1 até 10 faca
        tabuada <- numero * i
        escreval(numero, "x", i, "=", tabuada)
   fimpara
        

Fimalgoritmo